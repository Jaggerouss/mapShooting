# 高德接入方案（评分功能）

> 状态：**未实施**。当前跑的是腾讯位置服务，周边列表没有评分。
> 想做的时候把这份文档丢给 Claude，按第 5 节的清单直接改。
> 写于 2026-08-19。

---

## 1. 为什么要换

腾讯位置服务的地点搜索 `ws/place/v1/search` 返回的字段是：

```
id / title / address / category / type / tel / location{lat,lng} / _distance / ad_info
```

**没有评分，没有人均消费。** 这类数据在大众点评手里，腾讯的开放 API 不给。

高德 Web 服务的周边搜索加上 `extensions=all` 之后，`biz_ext` 里带评分和人均：

```json
{
  "name": "XX咖啡(青浦万达店)",
  "type": "餐饮服务;咖啡厅;咖啡厅",
  "address": "公园路100号",
  "location": "121.113021,31.151209",
  "distance": "420",
  "biz_ext": { "rating": "4.6", "cost": "38.00" }
}
```

餐饮类 POI 基本都有 `rating`，非餐饮（商场、公园）经常是空字符串，**代码必须容忍缺失**。

---

## 2. 已经预留好的接缝

位置服务调用全部收在 [`apps/dart-map/utils/lbs.js`](apps/dart-map/utils/lbs.js) 里，页面不直接碰任何服务商的 API。接口是：

```js
module.exports = {
  provider: "tencent",
  supportsRating: false,

  hasKey(),
  reverseGeocode(point)        // -> Promise<{ ok, address, reason }>
  countNearby(point, opts)     // -> Promise<{ ok, count, reason, message }>
  searchNearby(point, opts)    // -> Promise<{ ok, list, reason, message }>
};
```

`searchNearby` 返回的每一项已经带了 `rating` 和 `cost` 两个字段，**腾讯实现里恒为 `null`** —— 就是给这次替换留的位置。

也就是说：**换服务商 = 新写一个同接口的模块，`game.js` 一行不用改。**

---

## 3. 你要先去做的三件事

这三步 Claude 做不了，得你自己操作：

1. **申请 Key**：<https://lbs.amap.com> 注册 → 控制台 → 应用管理 → 创建应用 → 添加 Key，**服务平台必须选「Web服务」**（不是「微信小程序」，那个是给 JS SDK 用的，调不了 REST 接口）
2. **配合法域名**：微信公众平台 → 开发 → 开发设置 → 服务器域名 → request 合法域名加上
   ```
   https://restapi.amap.com
   ```
   （腾讯的 `https://apis.map.qq.com` 保留，逆地理编码还用它）
3. **给 Key 加白名单**：高德控制台里给这个 Key 绑定 IP 白名单或配额告警。**这条不是可选项** —— 见第 6 节。

---

## 4. 接口对照表

| 用途 | 腾讯（现在） | 高德（替换后） |
|---|---|---|
| 域名 | `https://apis.map.qq.com` | `https://restapi.amap.com` |
| 逆地理编码 | `/ws/geocoder/v1/` | `/v3/geocode/regeo` |
| 周边搜索 | `/ws/place/v1/search` | `/v3/place/around` |
| 坐标顺序 | **纬度在前** `lat,lng` | **经度在前** `lng,lat` |
| 半径参数 | `boundary=nearby(lat,lng,r)` | `radius=r`（米，最大 50000） |
| 成功判定 | `status === 0` | `status === "1"`（**字符串**） |
| 配额耗尽 | `status === 121` | `infocode === "10003"` |
| Key 无效 | `status === 110/111` | `infocode === "10001"` |
| 总数字段 | `count` | `count`（**字符串**，要 `Number()`） |
| 距离字段 | `_distance`（数字，米） | `distance`（**字符串**，米） |
| 评分 | 无 | `biz_ext.rating`（字符串，可能为 `""`） |
| 人均 | 无 | `biz_ext.cost`（字符串，可能为 `""`） |
| 按距离排序 | `orderby=_distance` | `sortrule=distance` |
| 分类过滤 | `keyword=美食` | `types=050000`（POI 分类码）或 `keywords=美食` |

### 高德周边搜索请求示例

```
GET https://restapi.amap.com/v3/place/around
  ?key=YOUR_KEY
  &location=121.113021,31.151209     经度在前！
  &keywords=咖啡
  &radius=2000
  &sortrule=distance                 或 weight（高德综合排序）
  &extensions=all                    不加这个就没有 biz_ext
  &offset=20                         每页条数，最大 25
  &page=1
```

### 常用 POI 分类码（比 keywords 更准）

| 场景 | `types` |
|---|---|
| 餐饮全类 | `050000` |
| 咖啡厅 | `050500` |
| 商场/购物中心 | `060100` |
| 电影院 | `080601` |
| 公园广场 | `110100` |

`types` 和 `keywords` 可以同时传，是「与」关系。

---

## 5. 改动清单

### 5.1 新建 `apps/dart-map/utils/lbs-amap.js`

照着 `lbs.js` 的接口写，注意这几个坑：

- 高德的 `status` 是**字符串** `"1"`，不是数字 `1`
- `location` 是 `"121.11,31.15"` 这种**逗号拼接的字符串**，要 `split(",")` 再 `Number()`
- `distance`、`count`、`rating`、`cost` 全是字符串，要转数字
- `biz_ext` 可能整个不存在，`rating` 可能是空字符串 `""`，判空要写成 `Number(x) || null` 而不是 `x || null`
- 高德的 `type` 形如 `"餐饮服务;咖啡厅;咖啡厅"`，用 `;` 分割取中间段比取末段好读

映射成统一格式：

```js
const list = (data.pois || []).map((p) => {
  const [lng, lat] = (p.location || ",").split(",").map(Number);
  const biz = p.biz_ext || {};
  const m = Number(p.distance) || 0;
  return {
    id: p.id,
    name: p.name,
    address: p.address,
    category: (p.type || "").split(";")[1] || "",
    distance: m,
    distanceText: m < 1000 ? Math.round(m) + "m" : (m / 1000).toFixed(1) + "km",
    latitude: lat,
    longitude: lng,
    rating: Number(biz.rating) || null,
    cost: Number(biz.cost) || null,
  };
});
```

模块尾部导出：

```js
module.exports = {
  provider: "amap",
  supportsRating: true,
  hasKey, reverseGeocode, countNearby, searchNearby,
};
```

### 5.2 `config.example.js` / `config.js` 加一个 Key

```js
module.exports = {
  TENCENT_MAP_KEY: "",
  AMAP_KEY: "",
};
```

`config.js` 已经在 `.gitignore` 里，不会误提交。

### 5.3 切换点

最省事的方式是在 `lbs.js` 里做转发，`game.js` 完全不用动：

```js
const amap = require("./lbs-amap.js");
const tencent = require("./lbs-tencent.js");   // 把现在的 lbs.js 改名
module.exports = amap.hasKey() ? amap : tencent;
```

高德 Key 没配就自动退回腾讯，不会白屏。

### 5.4 UI 要改的地方

`game.wxml` 的 `.poi` 行加评分展示，用 `supportsRating` 或直接判 `item.rating` 是否为空：

```html
<view class="poi-main">
  <text class="poi-name">{{item.name}}</text>
  <text class="poi-addr">{{item.category}} · {{item.address}}</text>
</view>
<view class="poi-side">
  <text wx:if="{{item.rating}}" class="poi-rating">{{item.rating}}</text>
  <text wx:if="{{item.cost}}" class="poi-cost">¥{{item.cost}}</text>
  <text class="poi-dist">{{item.distanceText}}</text>
</view>
```

### 5.5 排序策略

换到高德之后，`game.js` 的 `_loadNearby` 可以按 tab 用不同排序：

| tab | 排序 | 理由 |
|---|---|---|
| 吃饭 | 评分优先（`rating` 降序，无评分排最后） | 「评分高的饭店」这个需求本身 |
| 咖啡 | 距离优先 | 咖啡店同质化，近的赢 |
| 逛街 | 距离优先 | 商场没什么评分可言 |

注意：**别直接用 `sortrule=weight`**（高德的综合排序），它掺了商业权重，出来的不一定是评分高的。老实用 `sortrule=distance` 拉 25 条回来，本地按 `rating` 排。

---

## 6. ⚠️ 安全：这次别再犯同样的错

现在腾讯 Key 是打包进小程序代码包的，**反编译就能拿到**（这是 [项目分析.md](项目分析.md) 里的 P1-1，至今没修）。加一个高德 Key 等于把这个问题翻倍。

两个选择，**必须选一个**：

**A. 最低限度（10 分钟）**
- 高德控制台给 Key 绑 IP 白名单 / 域名白名单
- 设置每日调用量告警和上限
- 腾讯 Key 同样处理，绑 AppID 白名单

**B. 彻底方案（推荐）**
- 用微信云开发云函数或自己的服务端做中转，两个 Key 都只存在服务端
- 小程序只调自己的接口，客户端代码包里一个 Key 都没有
- 顺带还能加缓存 —— 同一个落点附近的搜索结果可以缓存，配额直接省一大截

---

## 7. 配额估算

| 动作 | 腾讯调用 | 高德调用 |
|---|---|---|
| 逆地理编码（选中候选） | 1 | 或 1（看你把 regeo 放哪边） |
| 周边探测（半径 1→3→5km 递进） | 1～3 | — |
| 打开一个 tab | — | 1 |
| 切到另一个 tab | — | 1（有本地缓存，切回来不重复） |

一次完整投掷常见路径 **3～4 次调用**。

高德个人开发者 Web 服务免费额度是 **每日 5000 次 / 3 QPS**（以官网当前政策为准，会变）。按一次投掷 4 次算，够玩 1000 多轮，个人使用完全够。

真要省，第 6 节 B 方案的服务端缓存最有效：落点坐标取整到小数点后 3 位（约 100m 网格）当缓存 key，同一片区域的搜索结果直接复用。

---

## 8. 保持腾讯做逆地理编码？

可以，两边混用没问题，`lbs.js` 转发层里按方法分流即可：

```js
module.exports = {
  provider: "mixed",
  supportsRating: true,
  hasKey: () => tencent.hasKey() || amap.hasKey(),
  reverseGeocode: tencent.reverseGeocode,   // 腾讯的中文地址描述更顺
  countNearby: amap.countNearby,
  searchNearby: amap.searchNearby,          // 要评分，必须走高德
};
```

腾讯的逆地理编码返回的中文地址（`result.address`）读起来比高德的 `formatted_address` 自然一些，保留它是合理的。代价是两边配额都要盯。
