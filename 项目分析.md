# mapShooting 项目分析报告

> 生成时间：2026-08-18
> 分析范围：仓库全部文件（HEAD = `79149ca upgrade appid`）

---

## 一、这个项目在做什么

一个**微信小程序单仓多应用（monorepo）**，核心产品是「**跟着 Jagger 去旅行**」——一个用「投飞镖」的方式随机决定去哪玩的小工具。

### 1.1 产品逻辑（当前代码的真实行为）

```
首页 index                          游戏页 game
┌──────────────────┐              ┌──────────────────┐
│ 背景图 + 标题     │              │ <map> 原生地图    │
│ picker 选范围：   │  navigateTo  │ 「投掷飞镖」按钮  │
│  上海市 / 青浦区  │ ?regionId=X  │                   │
│  / 华新镇         │─────────────>│ 点击 → 在该行政区 │
│ [开始游戏]        │              │ 边界内随机一个点  │
└──────────────────┘              │ → 打 marker+气泡  │
                                   │ → 逆地理编码出地名│
                                   │ → 底部结果卡片    │
                                   └──────────────────┘
```

**核心算法**（`pages/game/game.js`）：

1. `computeBBox()` — 算行政区 MultiPolygon 的外接矩形
2. `randomPointInRegion()` — **拒绝采样**：在 bbox 内均匀撒点，用射线法判断是否落在真实边界内，最多试 500 次
3. `pointInMultiPolygon / pointInPolygon / pointInRing` — 标准 ray-casting 点在多边形内判定，支持多岛屿（MultiPolygon）和挖洞（内环）
4. `reverseGeocode()` — 调腾讯位置服务 `apis.map.qq.com/ws/geocoder/v1/` 把经纬度换成地名

所以它**不是**真的物理投掷模拟，是「在指定行政区划内做一次面内均匀随机抽样」，飞镖只是交互皮肤。

### 1.2 仓库结构

| 路径 | 作用 |
|------|------|
| `project.config.json` | 开发者工具配置，`miniprogramRoot` 决定当前打开哪个 app |
| `apps/dart-map/` | 主应用：飞镖选地方 |
| `apps/test-app/` | 占位应用，仅用于验证「改一行配置切换小程序」这个方案可行 |
| `apps/dart-map/assets/geo/*.js` | 行政区 GeoJSON，包成 `module.exports = {...}`（小程序不能直接 require .json 大文件所以转成 .js） |
| `apps/dart-map/assets/geo/regions.js` | 区域注册表，新增区域改这一个文件 |
| `config.example.js` | 腾讯地图 Key 模板，真实 `config.js` 被 gitignore |

### 1.3 已注册的三个区域

| id (adcode) | 名称 | 级别 | 文件大小 |
|---|---|---|---|
| 310000 | 上海市 | province | 21 KB |
| 310118 | 青浦区 | district | 30 KB |
| 310118109 | 华新镇 | town | 10 KB |

---

## 二、问题清单

### 🔴 P0 —— 阻断级，代码现在跑不起来

#### P0-1 `config.js` 不存在，游戏页直接编译失败

`pages/game/game.js:5`
```js
const { TENCENT_MAP_KEY } = require("../../config.js");
```

但 `.gitignore` 里写了 `apps/*/config.js`，仓库里只有 `config.example.js`。**新克隆仓库的人，一进游戏页就是 `module "config.js" is not defined` 报错，整个页面白屏。** README / DEVNOTES 里都没写「必须先 cp config.example.js config.js」这一步。

修复二选一：
- 代码里做兜底（推荐，因为没 Key 也能玩）：
  ```js
  let TENCENT_MAP_KEY = "";
  try { TENCENT_MAP_KEY = require("../../config.js").TENCENT_MAP_KEY || ""; } catch (e) {}
  ```
- 或者把 `config.js` 从 gitignore 拿掉，提交一个空 Key 的版本

#### P0-2 首页背景图 `home_bg.jpg` 从来没存在过

`pages/index/index.wxml:4` 引用 `/assets/images/home_bg.jpg`，但 `assets/images/` 里**只有 `dart-pin.png`**。查 git 全历史，这个文件从未被提交过。首页背景永远是空白，只剩黑色蒙层。

（旧版本有 `china-map.svg` / `shanghai-map.svg` / `world-map.svg`，在 `896bcb5 test region` 那次提交里全被删了，但 wxml 引用没跟着清理。）

#### P0-3 两个音频文件都不存在

`initAudio()` 引用 `/assets/audio/bgm.mp3` 和 `dart_hit.mp3`，目录里只有一个 `请补充音频_README.txt`。

更麻烦的是 **bgm 没有 `onError` 处理**（sfx 有），加载失败会持续在控制台抛错：
```js
const bgm = wx.createInnerAudioContext();
bgm.src = "/assets/audio/bgm.mp3";   // 文件不存在
bgm.loop = true;
bgm.play();                           // ← 无 onError 兜底
```

---

### 🟠 P1 —— 严重，影响正确性 / 安全 / 可维护性

#### P1-1 【安全】腾讯地图 Key 打包进客户端，等于公开

小程序代码包可以被反编译，`config.js` 里的 `TENCENT_MAP_KEY` 会被任何人拿到并盗刷你的配额（代码里已经在处理 `status === 121` 配额耗尽的情况，说明这个风险是真实的）。

**必须做的加固**（腾讯位置服务控制台）：
- 给 Key 绑定 **WebServiceAPI 白名单**，限制来源 IP / 小程序 AppID
- 或者更彻底：把逆地理编码放到自己的云函数 / 服务端做中转，Key 不下发到客户端

#### P1-2 【文档】README 和 DEVNOTES 完全过时，描述的是已经删掉的旧版本

`896bcb5 test region` 那次提交把玩法从「上传图片 + Canvas + 摇杆」整个换成了「原生 map 组件 + 行政区随机点」，`game.js` 从 477 行重写成 285 行，但文档没同步。

现在文档里这些内容**全部是错的**：

| 文档里写的 | 代码实际情况 |
|---|---|
| 「上传地图图片，自动缩放」 | 没有上传功能，用的是原生 `<map>` |
| 「力度滑杆 + 方向摇杆」 | 已删除，只剩一个「投掷飞镖」按钮 |
| 「Canvas 绘制 / `drawMapImage()` / `paintFrame()`」 | 没有任何 Canvas 代码 |
| 「命中放大镜（放大 2.5 倍）」 | 不存在 |
| 「最近使用地图（Storage 缓存）」 | 首页是 picker 选行政区，没有 Storage |
| DEVNOTES 三条 Bug 修复记录（`throwAgain` 重绘 / `.recent-scroll` 滚动 / 摇杆 `boundingClientRect` 竞态） | 涉及的函数和样式类**全部已不存在** |
| DEVNOTES 技术架构表「Canvas 渲染流程」整节 | 已失效 |

README 也**完全没提**新版本必须的两件事：配置 `config.js`、在小程序后台把 `https://apis.map.qq.com` 加进 request 合法域名。

#### P1-3 `.DS_Store` 已被 git 跟踪，`.gitignore` 加晚了

```
.DS_Store
apps/dart-map/assets/.DS_Store
apps/dart-map/pages/.DS_Store
```
这三个文件在 `6e6695d Initial commit` 就进库了，`.gitignore` 是后来 `896bcb5` 才加的规则，对已跟踪文件无效。

```bash
git rm --cached .DS_Store apps/dart-map/assets/.DS_Store apps/dart-map/pages/.DS_Store
```

#### P1-4 首页启动时同步加载全部 GeoJSON（约 62 KB）

`pages/index/index.js:1-6` 在**模块顶层**就 require 了 `regions.js`，而 `regions.js` 又顶层 require 了三个 geo 文件。也就是说：**打开首页 = 解析 62 KB JSON**，而首页只需要三个名字。

区域越加越多，首页启动越慢。建议 `regions.js` 拆成「名字表（轻）」+「几何数据（懒加载）」：
```js
const REGION_META = [{ id: "310000", name: "上海市" }, ...];   // 首页只要这个
function loadGeo(id) { return require(`./${id}.js`); }          // 游戏页再加载
```

---

### 🟡 P2 —— 一般，逻辑瑕疵 / 死代码

#### P2-1 音乐开关按钮被 `wx:if="{{false}}"` 永久隐藏，但 BGM 还在自动播放

`game.wxml:4`
```html
<view wx:if="{{false}}" class="music-btn" bindtap="toggleMusic">
```
按钮被藏了，`toggleMusic()`、`musicOn`、`.music-btn` 样式全成了死代码。**用户没有任何办法关掉背景音乐**（一旦音频文件补齐，这就是个体验问题）。要么删干净，要么把按钮放出来。

#### P2-2 拒绝采样在「大范围 + 大量顶点」下有性能风险

`randomPointInRegion()` 每次尝试都要遍历整个 MultiPolygon 的所有环、所有顶点。上海市的 geojson 有几千个顶点、又是多岛屿 + 大量沿海水域，bbox 命中率不高，最坏情况 500 次尝试 × 数千次浮点运算，在低端机上会有明显卡顿（且是同步阻塞 UI 线程）。

优化方向：
- 每个 polygon 预算各自的 bbox，采样时先做 bbox 快速剔除，再跑射线法
- 或者预计算一遍，把区域切成网格缓存

#### P2-3 经纬度均匀采样 ≠ 面积均匀

直接在 `[minLng, maxLng] × [minLat, maxLat]` 上均匀取值，在球面上北边会比南边密。上海纬度跨度只有约 1°，实际偏差可以忽略，但如果以后加省级 / 国家级区域就会有可见偏差。修正方式：对 lat 按 `asin(uniform(sin(minLat), sin(maxLat)))` 采样。

#### P2-4 `.eslintrc.js` 实际上什么都没检查

```js
ecmaFeatures: { modules: true },   // ← 位置错了，应该在 parserOptions 里
// extends: 'eslint:recommended',  // ← 被注释掉
rules: {},                          // ← 空
```
没有 `extends`、没有 `rules`，这份配置跑起来零告警。而且仓库里没有 `package.json`，eslint 根本装不上。这个文件是微信开发者工具生成的模板，目前是纯装饰。

#### P2-5 `index.wxml` 里有开发者工具拖拽留下的垃圾内联样式

`index.wxml:29`
```html
<button class="start-btn" ... style="position: relative; left: 0rpx; top: 2rpx">
```
`left: 0rpx` 无意义，`top: 2rpx` 是误拖 2 像素。删掉。

#### P2-6 `noop()` 是未使用的死函数

`game.js:284` 定义了 `noop() {}`，wxml 里没有任何地方绑定它（推测原本想给结果面板做 `catchtap` 阻止穿透，后来没用上）。

#### P2-7 `test-app` 的「应用列表」是假的

`apps/test-app/pages/home/home.js` 里的 `appList` 是硬编码的静态数组，卡片点了没反应，只是文字展示。它连「跳转到另一个小程序」都做不到（小程序之间要 `wx.navigateToMiniProgram` + 各自独立 AppID）。这个 app 的唯一价值是证明 `miniprogramRoot` 切换有效，README 里写清楚就行，别让人误以为是个启动器。

#### P2-8 缺少 `package.json`

没有依赖管理、没有 lint / format 脚本、没有 CI。对纯原生小程序不是必须，但 eslint 配置摆在那里却装不上，属于配置和现实脱节。

---

### 🔵 P3 —— 提示

- `project.config.json` 里的 AppID `wx011dd4ef400bc850` 是真实 AppID 并已入库。小程序 AppID 本身不是机密（客户端可见），但换成别人的仓库要记得改。
- `app.js` 的 `onLaunch` 是空的，只有注释占位。
- `throwDart()` 里 marker 固定 `id: 1`，每次投掷会替换上一个 —— 这是**符合预期**的（只显示当前这一镖），不是 bug，只是如果以后要做「历史记录 / 多镖对比」需要改成递增 id。
- `onMapError` / `onMapAbilityFail` 用 `wx.showModal` + `JSON.stringify` 把原始错误糊用户脸上，是调试期的写法，上线前应该换成友好文案。
- 逆地理编码没有防抖 / 节流。虽然按钮有 `disabled="{{throwing}}"` 挡着，但如果网络请求挂起很久，用户只能干等，没有超时提示。

---

## 三、修复优先级建议

| 顺序 | 事项 | 理由 |
|---|---|---|
| 1 | `config.js` 加 try/catch 兜底 | 不修就是白屏，谁 clone 都跑不起来 |
| 2 | 补 `home_bg.jpg`，或从 wxml 删掉这个 image | 首页现在是黑的 |
| 3 | 补音频，或删掉 `initAudio()` 整段 | 控制台一直报错 |
| 4 | 腾讯地图 Key 加 AppID 白名单 | **安全**，不加会被盗刷 |
| 5 | 重写 README + DEVNOTES | 现在的文档误导性大于零信息 |
| 6 | `git rm --cached` 三个 `.DS_Store` | 一行命令的事 |
| 7 | `regions.js` 改懒加载 | 加区域之前改，成本最低 |
| 8 | 清死代码（`noop`、`musicOn`、内联 style、`.eslintrc.js`） | 顺手 |

---

## 四、总结

**做得好的地方**：拒绝采样 + 射线法的实现是正确的，MultiPolygon 多岛屿和内环挖洞都处理了；`regions.js` 注册表的设计让「加一个新区域」只需要改一个文件；单仓多小程序靠 `miniprogramRoot` 切换的方案简单有效；逆地理编码的错误分支（无 Key / status 121 配额耗尽 / 其他 status / 网络失败）分得很细。

**最大的问题**：**这个仓库处于「代码已经重写完，但周边全没跟上」的状态**。`896bcb5` 那次提交把玩法整个换掉了，却留下了三份和代码对不上的东西 —— 过时的文档、失效的资源引用、必需但缺失的 `config.js`。结果就是**一个新人 clone 下来，跑不起来，而且按文档做也跑不起来**。

代码本身的质量（P2 那批）都是小瑕疵，真正要花力气的是把文档和资源补齐，让项目重新变成「clone 下来能跑」的状态。

---

## 五、本次改动记录（2026-08-19）

### 已修

| 编号 | 事项 | 做法 |
|---|---|---|
| P0-1 | `config.js` 缺失导致白屏 | `require` 包 try/catch，降级为无 Key 模式 |
| — | 新增投掷动画 | 蓄力力度条 → 飞镖飞行 → 命中冲击波 + 抖屏 + 镜头推近 |
| — | 新增三选一 | 一镖落地震出 3 个候选，选中后才做逆地理编码 |
| — | 力度有了实际含义 | power = 距市中心的归一化距离，轻投落市区、重投落远郊 |
| — | 初始视野自适应 | 原来三个区域都写死 `scale: 10`，镇级看不清、市级看不全；改为按 bbox 长边算 |
| P2-1 半 | bgm 缺 `onError` | 补上，音频文件缺失不再刷屏（音乐开关按钮仍是隐藏状态，未处理） |
| — | 位置服务抽成适配层 | 新增 `utils/lbs.js`，页面不再直连腾讯 API，为换服务商留接缝 |
| — | 新增确认弹窗 | 选中候选后先探测周边再问「行不行」，荒地会明说 |
| — | 新增周边推荐 | 咖啡 / 吃饭 / 逛街三个 tab，懒加载 + 本地缓存，点击拉起导航 |

> 周边列表**没有评分** —— 腾讯地点搜索不返回该字段。要评分需换高德，方案见 [高德接入方案.md](高德接入方案.md)。

### 订正：P2-2 的性能担忧不成立

前面写「最坏 500 次尝试 × 数千顶点，低端机会明显卡顿」——实测下来是错的：

| 区域 | polygon 数 | 顶点数 | bbox 命中率 | 单次点在多边形判定 |
|---|---|---|---|---|
| 上海市 | 11 | 915 | 44.9% | 0.004 ms |
| 青浦区 | 1 | 1205 | 38.5% | 0.003 ms |
| 华新镇 | 1 | 383 | 63.9% | 0.003 ms |

顶点数是**几百到一千**，不是「数千」。加上力度环带约束后凑满 3 个候选，120 次实测全部成功，单次耗时 **0.0–0.4 ms**。

结论：**采样性能不需要优化**，前面建议的「每 polygon 预算 bbox」「网格缓存」都是过度设计，不用做。

### 仍未处理

- **P1-1【安全】腾讯 Key 打包进客户端** —— 优先级最高，必须去控制台绑 AppID 白名单
- P0-2 `home_bg.jpg` 缺失（首页仍是黑的）
- P0-3 音频文件缺失
- P1-2 README / DEVNOTES 过时（这次改完后差得更远了）
- P1-3 `.DS_Store` 已被跟踪
- P1-4 首页启动同步加载全部 GeoJSON
- P2-1 音乐开关按钮 `wx:if="{{false}}"`
- P2-4 `.eslintrc.js` 空配置
- P2-5 `index.wxml` 拖拽残留内联样式
